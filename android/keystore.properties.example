# Copiar a keystore.properties (sin el .example) y llenar con los datos
# reales. keystore.properties y el .jks/.keystore NUNCA se commitean - ver
# android/.gitignore y el README para el procedimiento de generacion.
storeFile=../mirage-release.keystore
storePassword=
keyAlias=
keyPassword=
